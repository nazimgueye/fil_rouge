pipeline {
  agent any
  stages {
    stage ('test') {
      steps{
        sh 'sudo echo "test okkkkkkkkk"'
        sh 'apt install docker' 
        sh 'docker ps -a'
      }
    }
    stage ('Run Docker Compose') {
      steps{
        sh 'docker-compose up  -d'
      }
    }
  }
  
}
