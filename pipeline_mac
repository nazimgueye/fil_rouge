pipeline {
  agent any
  stages {
    stage ('test') {
      steps{
        sh 'docker version'
      }
    }
    stage ('Run Docker Compose') {
      steps{
        sh 'docker-compose up  -d'
      }
    }
  }
  
}
