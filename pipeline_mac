pipeline {
  agent any
  stages {
    stage ('test') {
      steps{
        sh 'sudo docker ps -a'
      }
    }
    stage ('Run Docker Compose') {
      steps{
        sh 'sudo docker-compose up  -d'
      }
    }
  }
  
}