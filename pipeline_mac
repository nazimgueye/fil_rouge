pipeline {
  agent any
  stages {
    stage ('test') {
      steps{
        sh 'echo "test okkkkkkkkk"'
        sh 'docker ps -a'
      }
    }
    stage ('Run Docker Compose') {
      steps{
        sh 'docker-compose up  -d'
      }
    }
  }
  
}
