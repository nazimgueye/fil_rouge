pipeline {
  agent any
  stages {
    stage ('test') {
      steps{
       bash 'docker version'

      }
    }
    stage ('Run Docker Compose') {
      steps{
        sh 'docker-compose up  -d'
      }
    }
  }
  
}
