pipeline {
    agent any
    stages {
        stage('Build images') {
            steps {
                sh 'docker version'
            }
        }
        stage('Tag images') {
            steps {
                docker build -t test1-db:1.0 Db.Dockerfile
            }
        }
    }
}
