pipeline {
    agent any
    stages {
        stage('Build images') {
            steps {
                sh 'docker version'
            }
        }
        stage('Tag images') {
            steps {
                //sh 'docker-compose up -d --build'
            }
        }
    }
    post {
        success {
            script {
                mail bcc: '', body: 'Test envoi mail', subject: 'Test', to: 'nazimgueye17@gmail.com'
            }
        }
    }
}
