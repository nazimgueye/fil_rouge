apiVersion: v1
kind: Service
metadata:
  name: php-service
spec:
  selector:
    app: php-app
  ports:
    - protocol: TCP
      port: 8585
      targetPort: 80
  type: LoadBalancer
  #  externalIPs:
  #    - 127.0.0.1

